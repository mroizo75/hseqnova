import { CORE_HSEQ_NAV_HREFS } from "@/lib/dashboard-nav-config";

/** @deprecated Industry does not change the UK menu. Use CORE_HSEQ_NAV_HREFS. */
export const BASE_SIMPLE_MODULES: string[] = [...CORE_HSEQ_NAV_HREFS];
